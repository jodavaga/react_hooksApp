export const todoReducer = (state, action) => {
    
    switch (action.type) {
        // case 'agregar':
        //     break;
    
        default:
            return state;
            break;
    }
}